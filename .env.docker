# ============================================================
# FILE: .env.docker (for Docker Compose, place in project root)
# ============================================================
# Used by docker-compose.yml for container environment

ENVIRONMENT=development
DEBUG=true

# Note: In Docker, service names are hostnames
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/ecg
REDIS_URL=redis://redis:6379
MLFLOW_TRACKING_URI=http://mlflow:5000

MODEL_VERSION=latest
MODEL_PATH=/app/models

# Data path inside container (mount via volume)
PTB_XL_PATH=/data/ptb-xl
